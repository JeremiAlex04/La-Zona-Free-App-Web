import React from "react";

const Piece = ({ 
    // eslint-disable-next-line no-unused-vars
    piece, 
    // eslint-disable-next-line no-unused-vars
    position 
}) => {
    //Componente para manejar piezas individuales
    //Este componente no esta implementado todavia, por eso no se usan las props

    return (
        <div className="piece">
            {/*Renderizado de la pieza*/}
        </div>
    );
};

export default Piece;